---
phase: 01-ds-compliance-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(onboarding)/onboarding/wizard/[step]/page.tsx
  - app/(protected)/settings/page.tsx
  - components/brand/sections/ProfileSection.tsx
  - components/brand/sections/VoiceSection.tsx
  - components/brand/sections/AiToolsSection.tsx
  - components/campaigns/CampaignSetup.tsx
  - components/knowledge/ImportFlow.tsx
  - components/PostEditorModal.tsx
autonomous: true
requirements:
  - DS-FORMS-01
  - DS-FORMS-02
  - DS-FORMS-03
  - DS-FORMS-04

must_haves:
  truths:
    - All form inputs in priority files render with DS styling
    - No raw unstyled form elements appear in onboarding, settings, or brand sections
    - Form field labels and validation states are visible
    - Form values can be entered and retrieved correctly
  artifacts:
    - app/(onboarding)/onboarding/wizard/[step]/page.tsx (0 raw input/select/textarea)
    - app/(protected)/settings/page.tsx (0 raw input/select)
    - components/brand/sections/ProfileSection.tsx (uses DS Input)
    - components/brand/sections/VoiceSection.tsx (uses DS Input/Textarea)
    - components/brand/sections/AiToolsSection.tsx (uses DS Input)
    - components/campaigns/CampaignSetup.tsx (uses DS Input/Select)
    - components/knowledge/ImportFlow.tsx (uses DS Input)
    - components/PostEditorModal.tsx (uses DS Input/Select/Textarea)
  key_links:
    - from: Form inputs
      to: DS Input/Select/Textarea components
      pattern: "import.*Input.*from.*@doctorproject/react"
      critical: true
    - from: Form styling
      to: DS tokens
      pattern: "var\\(--drp-"
      critical: true
---

<objective>
Replace 60+ raw form inputs, selects, and textareas with DS `Input`, `Select`, and `Textarea` components across 8 priority files. This is the largest DS compliance gap and affects the highest-value user flows (onboarding, settings, content creation).

Purpose: Ensure form inputs across priority user flows render with consistent DS styling, borders, and validation states — eliminating the visual inconsistency of browser-default unstyled inputs.

Output: All form elements in priority files use DS components with consistent styling and behavior.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@/Users/y/Desktop/doctorpost-v12/CLAUDE.md
@/Users/y/Desktop/doctorpost-v12/.planning/phases/ds-audit/ds-UAT.md

# Design system reference

DS components available: `Input`, `Select`, `Textarea` from `@doctorproject/react`
DS tokens: `var(--drp-border)`, `var(--drp-text-muted)`, `var(--drp-error)` for validation states

# Current form element counts per file (from audit)

- app/(onboarding)/onboarding/wizard/[step]/page.tsx — 10 raw inputs
- components/campaigns/CampaignSetup.tsx — 7 raw inputs/selects
- components/brand/sections/VoiceSection.tsx — 6 raw inputs/textareas
- components/brand/sections/ProfileSection.tsx — 6 raw inputs
- app/(protected)/settings/page.tsx — 6 raw inputs/selects
- components/knowledge/ImportFlow.tsx — 4 raw inputs
- components/brand/sections/AiToolsSection.tsx — 4 raw inputs
- components/PostEditorModal.tsx — 4 raw inputs/selects/textareas
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Replace form inputs in onboarding wizard and settings pages</name>
  <files>
    - app/(onboarding)/onboarding/wizard/[step]/page.tsx
    - app/(protected)/settings/page.tsx
  </files>
  <read_first>
    - app/(onboarding)/onboarding/wizard/[step]/page.tsx (see current raw input structure, count them)
    - app/(protected)/settings/page.tsx (see current raw input/select structure)
    - /Users/y/Desktop/doctorpost-v12/CLAUDE.md (DS component import syntax and rules)
  </read_first>
  <action>
Replace all 16 raw `<input>` and `<select>` elements in these two files with DS components.

**Onboarding wizard ([step]/page.tsx):** 10 raw inputs

- Import: `import { Input, Select } from "@doctorproject/react";`
- Replace each raw `<input type="text" />` with `<Input />` preserving value, onChange, placeholder, disabled, required
- Replace each raw `<input type="email" />` with `<Input type="email" />`
- Replace each raw `<input type="password" />` with `<Input type="password" />`
- Replace each raw `<input type="number" />` with `<Input type="number" />`
- Replace raw `<select>` with DS `<Select options={[...]} value={} onChange={} />`

**Settings page:** 6 raw inputs + selects

- Import: `import { Input, Select } from "@doctorproject/react";`
- Apply same replacement pattern for all inputs and selects
- Preserve existing state management, validation, error display logic
- Preserve existing onChange handlers and value bindings

**DO NOT:**

- Change the component's state logic or value handling
- Modify validation rules or error messages
- Alter the form submission flow
- Add new DOM elements beyond the component replacements

**Preserve all styling context:** Any existing inline styles for layout (gap, margin, padding) should remain; DS components handle their own border and text styling via tokens.
</action>
<verify>
<automated> # Check that raw input count drops to 0 in onboarding wizard
grep -c '<input' app/\(onboarding\)/onboarding/wizard/\[step\]/page.tsx 2>/dev/null || echo "0"

      # Check that raw input count drops to 0 in settings
      grep -c '<input' app/\(protected\)/settings/page.tsx 2>/dev/null || echo "0"

      # Verify DS Input import exists in both files
      grep -l "import.*Input.*from.*@doctorproject/react" app/\(onboarding\)/onboarding/wizard/\[step\]/page.tsx app/\(protected\)/settings/page.tsx

      # Verify no raw <select> tags remain
      grep -c '<select' app/\(onboarding\)/onboarding/wizard/\[step\]/page.tsx app/\(protected\)/settings/page.tsx 2>/dev/null || echo "0"
    </automated>

  </verify>
  <done>
- app/(onboarding)/onboarding/wizard/[step]/page.tsx contains 0 raw `<input>` elements (all replaced with DS `<Input>`)
- app/(protected)/settings/page.tsx contains 0 raw `<input>` and `<select>` elements
- Both files import `Input` and `Select` from `@doctorproject/react`
- Form functionality and state management unchanged
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace form inputs in brand sections (Profile, Voice, AiTools)</name>
  <files>
    - components/brand/sections/ProfileSection.tsx
    - components/brand/sections/VoiceSection.tsx
    - components/brand/sections/AiToolsSection.tsx
  </files>
  <read_first>
    - components/brand/sections/ProfileSection.tsx (see current raw inputs)
    - components/brand/sections/VoiceSection.tsx (see current raw inputs and textareas)
    - components/brand/sections/AiToolsSection.tsx (see current raw inputs)
    - /Users/y/Desktop/doctorpost-v12/CLAUDE.md (DS component import syntax)
  </read_first>
  <action>
Replace all 16 raw form elements across 3 brand section files with DS components.

**ProfileSection.tsx:** 6 raw inputs

- Import: `import { Input } from "@doctorproject/react";`
- Replace each raw `<input>` with DS `<Input />` preserving value, onChange, placeholder, validation
- Preserve any existing label and error message rendering

**VoiceSection.tsx:** 6 raw inputs + textareas (known to have `color: "#dc2626"` error styling — will fix in Plan 02)

- Import: `import { Input, Textarea } from "@doctorproject/react";`
- Replace 4 raw `<input>` elements with DS `<Input />`
- Replace 2 raw `<textarea>` elements with DS `<Textarea />` preserving rows, cols, value, onChange
- Preserve existing validation and error display (color issue will be fixed separately)

**AiToolsSection.tsx:** 4 raw inputs

- Import: `import { Input } from "@doctorproject/react";`
- Replace all 4 raw `<input>` with DS `<Input />`

**DO NOT:**

- Modify state management or validation logic
- Change the section's layout structure
- Alter the form submission behavior
- Fix color violations (that's handled in Plan 02)

**Preserve context:** Keep all parent container styling, gaps, padding, and labels intact.
</action>
<verify>
<automated> # Check raw input count drops to 0 in ProfileSection
grep -c '<input' components/brand/sections/ProfileSection.tsx 2>/dev/null || echo "0"

      # Check raw input and textarea counts drop to 0 in VoiceSection
      grep -c '<input\|<textarea' components/brand/sections/VoiceSection.tsx 2>/dev/null || echo "0"

      # Check raw input count drops to 0 in AiToolsSection
      grep -c '<input' components/brand/sections/AiToolsSection.tsx 2>/dev/null || echo "0"

      # Verify DS imports in all three files
      grep -l "import.*Input.*from.*@doctorproject/react" components/brand/sections/ProfileSection.tsx components/brand/sections/VoiceSection.tsx components/brand/sections/AiToolsSection.tsx

      # Verify Textarea import in VoiceSection
      grep "import.*Textarea.*from.*@doctorproject/react" components/brand/sections/VoiceSection.tsx
    </automated>

  </verify>
  <done>
- components/brand/sections/ProfileSection.tsx contains 0 raw `<input>` elements
- components/brand/sections/VoiceSection.tsx contains 0 raw `<input>` and `<textarea>` elements
- components/brand/sections/AiToolsSection.tsx contains 0 raw `<input>` elements
- All three files import the necessary DS components (`Input`, `Textarea` where used)
- Form validation and error messages preserved
  </done>
</task>

<task type="auto">
  <name>Task 3: Replace form inputs in campaign and knowledge components</name>
  <files>
    - components/campaigns/CampaignSetup.tsx
    - components/knowledge/ImportFlow.tsx
    - components/PostEditorModal.tsx
  </files>
  <read_first>
    - components/campaigns/CampaignSetup.tsx (see 7 raw input/select elements)
    - components/knowledge/ImportFlow.tsx (see 4 raw inputs)
    - components/PostEditorModal.tsx (see 4 raw input/select/textarea elements)
    - /Users/y/Desktop/doctorpost-v12/CLAUDE.md (DS component import syntax)
  </read_first>
  <action>
Replace all 15 raw form elements across 3 component files with DS components.

**CampaignSetup.tsx:** 7 raw inputs/selects

- Import: `import { Input, Select } from "@doctorproject/react";`
- Replace all raw `<input>` elements with DS `<Input />` preserving type, value, onChange, placeholder
- Replace all raw `<select>` elements with DS `<Select options={[...]} value={} onChange={} />`
- Preserve any fieldset/label grouping and validation context

**ImportFlow.tsx:** 4 raw inputs

- Import: `import { Input } from "@doctorproject/react";`
- Replace all 4 raw `<input>` with DS `<Input />` preserving type (text, email, etc.), value, onChange
- Preserve the import flow's state and submission logic

**PostEditorModal.tsx:** 4 raw input/select/textarea

- Import: `import { Input, Select, Textarea } from "@doctorproject/react";`
- Replace raw `<input>` with DS `<Input />`
- Replace raw `<select>` with DS `<Select />`
- Replace raw `<textarea>` with DS `<Textarea />`
- Preserve modal's internal state management and event handlers

**DO NOT:**

- Change component state logic or value bindings
- Modify validation or error display
- Alter form submission behavior
- Restructure component hierarchy

**Preserve:** All styling for layout (gaps, padding, margin) and label rendering.
</action>
<verify>
<automated> # Check raw form element counts in CampaignSetup
grep -c '<input\|<select' components/campaigns/CampaignSetup.tsx 2>/dev/null || echo "0"

      # Check raw input count in ImportFlow
      grep -c '<input' components/knowledge/ImportFlow.tsx 2>/dev/null || echo "0"

      # Check raw form element counts in PostEditorModal
      grep -c '<input\|<select\|<textarea' components/PostEditorModal.tsx 2>/dev/null || echo "0"

      # Verify DS imports exist
      grep -l "import.*Input.*from.*@doctorproject/react" components/campaigns/CampaignSetup.tsx components/knowledge/ImportFlow.tsx components/PostEditorModal.tsx

      # Verify Textarea and Select imports where needed
      grep "import.*Select.*from.*@doctorproject/react" components/campaigns/CampaignSetup.tsx
      grep "import.*Textarea.*from.*@doctorproject/react" components/PostEditorModal.tsx
    </automated>

  </verify>
  <done>
- components/campaigns/CampaignSetup.tsx contains 0 raw `<input>` and `<select>` elements
- components/knowledge/ImportFlow.tsx contains 0 raw `<input>` elements
- components/PostEditorModal.tsx contains 0 raw `<input>`, `<select>`, and `<textarea>` elements
- All necessary DS components imported in each file
- Form state management and submission flows unchanged
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **Form input compliance check:**

   ```bash
   # Combined count of raw form elements in all 8 priority files — should be 0 or only hidden/file/checkbox/radio
   grep -r '<input\|<select\|<textarea' \
     app/\(onboarding\)/onboarding/wizard/\[step\]/page.tsx \
     app/\(protected\)/settings/page.tsx \
     components/brand/sections/ProfileSection.tsx \
     components/brand/sections/VoiceSection.tsx \
     components/brand/sections/AiToolsSection.tsx \
     components/campaigns/CampaignSetup.tsx \
     components/knowledge/ImportFlow.tsx \
     components/PostEditorModal.tsx \
     2>/dev/null | grep -v 'type="hidden"\|type="file"\|type="checkbox"\|type="radio"' | wc -l
   # Expected: 0
   ```

2. **DS component imports:**

   ```bash
   # All priority files should import Input, Select, or Textarea from DS
   grep -l "@doctorproject/react" \
     app/\(onboarding\)/onboarding/wizard/\[step\]/page.tsx \
     app/\(protected\)/settings/page.tsx \
     components/brand/sections/*.tsx \
     components/campaigns/CampaignSetup.tsx \
     components/knowledge/ImportFlow.tsx \
     components/PostEditorModal.tsx
   ```

3. **No broken form state:**
   - Verify app starts without console errors related to undefined onChange handlers
   - Verify form values persist when changing and re-entering form fields
   - Verify form submission handlers still execute correctly
     </verification>

<success_criteria>

- [ ] **DS-FORMS-01 ✓**: All `<input>` elements in priority files replaced with DS `<Input>` or styled with `border: var(--drp-border)`
- [ ] **DS-FORMS-02 ✓**: All `<select>` elements in priority files replaced with DS `<Select>`
- [ ] **DS-FORMS-03 ✓**: All `<textarea>` elements in priority files replaced with DS `<Textarea>`
- [ ] **DS-FORMS-04 ✓**: Onboarding wizard, settings, brand sections (Profile, Voice, AiTools), campaigns setup, knowledge import, post editor modal all use DS form components
- [ ] Zero raw unstyled form elements across all 8 files
- [ ] Form functionality unchanged — values bind, validation displays, submission works
- [ ] All components import from `@doctorproject/react` correctly
      </success_criteria>

<output>
After completion, create `.planning/phases/01-ds-compliance-fixes/01-01-SUMMARY.md` with:
- Files modified (list)
- Inputs replaced per file (counts)
- Form functionality verified
- Ready for Plan 01-02 (color token replacement)
</output>
