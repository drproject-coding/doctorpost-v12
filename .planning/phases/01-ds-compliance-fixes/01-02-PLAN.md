---
phase: 01-ds-compliance-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/signup/page.tsx
  - components/brand/sections/VoiceSection.tsx
  - components/factory/DraftEditor.tsx
  - components/campaigns/CampaignAnalytics.tsx
  - components/factory/FormattedOutput.tsx
  - app/(onboarding)/onboarding/wizard/[step]/page.tsx
  - app/(protected)/studio/page.tsx
  - components/campaigns/IdeaStatusBadge.tsx
autonomous: true
requirements:
  - DS-COLORS-01
  - DS-COLORS-02
  - DS-COLORS-03

must_haves:
  truths:
    - Error states display in DS error red not Tailwind red
    - Auth pages show correct validation colors
    - Badge and status colors use DS tokens
    - Campaign analytics displays with DS color palette
  artifacts:
    - app/signup/page.tsx (no #b91c1c, uses var(--drp-error))
    - components/brand/sections/VoiceSection.tsx (no #dc2626, uses var(--drp-error))
    - components/factory/DraftEditor.tsx (no #dc2626, uses var(--drp-error))
    - components/campaigns/CampaignAnalytics.tsx (all hex colors replaced with DS tokens)
    - components/factory/FormattedOutput.tsx (all hex colors replaced with DS tokens)
    - app/(onboarding)/onboarding/wizard/[step]/page.tsx (no inline hex colors)
    - app/(protected)/studio/page.tsx (no inline hex colors)
    - components/campaigns/IdeaStatusBadge.tsx (status colors use DS tokens)
  key_links:
    - from: Error feedback
      to: var(--drp-error)
      pattern: "var\\(--drp-error\\)"
      critical: true
    - from: Color consistency
      to: DS token system
      pattern: "var\\(--drp-"
      critical: true
---

<objective>
Replace ~40 hardcoded hex color values with DS color tokens across 8 files. Priority: auth error colors, campaign analytics colors, badge/status colors.

Purpose: Eliminate visual inconsistencies caused by hardcoded hex values that don't follow the DS color palette. Ensure error states use `var(--drp-error)` instead of Tailwind red. Establish single source of truth for all colors via DS tokens.

Output: All inline color styles use `var(--drp-*)` tokens; no hex colors in component code.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@/Users/y/Desktop/doctorpost-v12/CLAUDE.md
@/Users/y/Desktop/doctorpost-v12/.planning/phases/ds-audit/ds-UAT.md

# DS Color Token Mapping (from audit + CLAUDE.md)

- Primary brand: `#631DED` → `var(--drp-purple)`
- Secondary brand: `#FF6C01` → `var(--drp-orange)`
- Error red: `#b91c1c`, `#dc2626`, `#FF4444` → `var(--drp-error)`
- Success green: `#00A896`, `#00AA00` → `var(--drp-success)`
- Warning yellow: `#D4A800`, `#D4A8001A` → `var(--drp-warning)`
- Teal/active: `#00A896` → `var(--drp-success)` or custom teal
- Muted text: `#888`, `#888888`, `#666`, `#666666` → `var(--drp-text-muted)`
- Black: `#000000`, `#121212` → `var(--drp-black)`
- Surface: `#F2F2F2` → `var(--drp-surface)`

# Files with hex color violations (from audit)

1. app/signup/page.tsx:179 — `color: "#b91c1c"` (error red)
2. components/brand/sections/VoiceSection.tsx:337 — `color: "#dc2626"` (error red)
3. components/factory/DraftEditor.tsx:29 — `#dc2626` (error red)
4. components/campaigns/CampaignAnalytics.tsx — 10 inline hex colors
5. components/factory/FormattedOutput.tsx — 8 inline hex colors
6. app/(onboarding)/onboarding/wizard/[step]/page.tsx — 5 inline hex colors
7. app/(protected)/studio/page.tsx — 5 inline hex colors
8. components/campaigns/IdeaStatusBadge.tsx — 5 inline hex colors
   </context>

<tasks>

<task type="auto">
  <name>Task 1: Replace error colors in auth and validation contexts</name>
  <files>
    - app/signup/page.tsx
    - components/brand/sections/VoiceSection.tsx
    - components/factory/DraftEditor.tsx
  </files>
  <read_first>
    - app/signup/page.tsx (find line 179 with `color: "#b91c1c"`, see all error state color usage)
    - components/brand/sections/VoiceSection.tsx (find line 337 with `color: "#dc2626"`, see validation styling)
    - components/factory/DraftEditor.tsx (find line 29 with `#dc2626`, see error styling context)
    - /Users/y/Desktop/doctorpost-v12/CLAUDE.md (DS error token: --drp-error)
  </read_first>
  <action>
Replace all auth and validation error colors with DS error token.

**app/signup/page.tsx:**

- Line 179: Replace `color: "#b91c1c"` with `color: "var(--drp-error)"`
- Search for all other Tailwind red colors (`#dc2626`, `#ef4444`, etc.) and replace with `var(--drp-error)`
- Verify error message text uses DS error color
- Preserve the error message styling context (font size, weight)

**components/brand/sections/VoiceSection.tsx:**

- Line 337: Replace `color: "#dc2626"` with `color: "var(--drp-error)"`
- Search file for other hardcoded red colors and replace with `var(--drp-error)`
- Preserve validation error display logic

**components/factory/DraftEditor.tsx:**

- Line 29: Replace `#dc2626` with `var(--drp-error)`
- Search for other error color references and replace
- Preserve problem state styling logic

**DO NOT:**

- Change error message text or validation logic
- Modify component structure or state management
- Alter validation display behavior

**Pattern:** Any color value used for error feedback (validation, problem state, error messages) → `var(--drp-error)`
</action>
<verify>
<automated> # Verify no hardcoded red colors (#b91c1c, #dc2626, #ef4444, etc.) remain in auth/validation files
grep -E '#(b91c1c|dc2626|ef4444|ff4444)' app/signup/page.tsx components/brand/sections/VoiceSection.tsx components/factory/DraftEditor.tsx 2>/dev/null || echo "0 hardcoded red colors found"

      # Verify DS error token is used instead
      grep -l "var(--drp-error)" app/signup/page.tsx components/brand/sections/VoiceSection.tsx components/factory/DraftEditor.tsx

      # Confirm no other inline color properties with hex values in error contexts
      grep 'color.*:.*"#' app/signup/page.tsx 2>/dev/null || echo "No hex colors in signup"
    </automated>

  </verify>
  <done>
- app/signup/page.tsx contains 0 hardcoded red colors; error feedback uses `var(--drp-error)`
- components/brand/sections/VoiceSection.tsx contains 0 hardcoded red colors; validation errors use `var(--drp-error)`
- components/factory/DraftEditor.tsx contains 0 hardcoded red colors; problem state uses `var(--drp-error)`
- All three files import and use DS error token correctly
- Error message display and validation logic preserved
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace campaign and analytics hex colors</name>
  <files>
    - components/campaigns/CampaignAnalytics.tsx
    - components/campaigns/IdeaStatusBadge.tsx
    - components/factory/FormattedOutput.tsx
  </files>
  <read_first>
    - components/campaigns/CampaignAnalytics.tsx (identify 10 inline hex colors, map each to DS token)
    - components/campaigns/IdeaStatusBadge.tsx (identify 5 status color hex values)
    - components/factory/FormattedOutput.tsx (identify 8 inline hex colors, determine context of each)
    - /Users/y/Desktop/doctorpost-v12/CLAUDE.md (DS color token reference)
  </read_first>
  <action>
Replace all hardcoded hex colors with DS color tokens based on their semantic purpose.

**Mapping strategy:** For each hex color, determine its semantic purpose, then map:

- Purple/brand colors (#631DED, similar) → `var(--drp-purple)`
- Orange/secondary (#FF6C01) → `var(--drp-orange)`
- Red/error (#dc2626, #FF4444) → `var(--drp-error)`
- Green/success (#00AA00, #00A896) → `var(--drp-success)`
- Yellow/warning (#D4A800, #FAE8A4) → `var(--drp-warning)`
- Grey/muted (#888888, #666666) → `var(--drp-text-muted)`
- Black (#000000, #121212) → `var(--drp-black)`

**components/campaigns/CampaignAnalytics.tsx:**

- Identify each of the 10 inline hex colors
- Replace with corresponding DS token based on color semantic (e.g., if showing status, use appropriate status color token)
- Preserve chart/graph rendering logic
- Preserve data display structure

**components/campaigns/IdeaStatusBadge.tsx:**

- Replace all 5 status color hex values with DS status tokens
- Example: Draft grey → `var(--drp-surface)`, To Review orange → `var(--drp-orange)`, Scheduled teal → `var(--drp-success)`, Published green → `var(--drp-success)`
- Preserve status badge display logic
- Maintain badge styling (border, background, text contrast)

**components/factory/FormattedOutput.tsx:**

- Replace all 8 inline hex colors with DS tokens
- Determine purpose of each (syntax highlighting, status, annotation, etc.) and map to appropriate token
- Preserve code formatting and output structure

**DO NOT:**

- Change component logic or data structure
- Modify chart rendering or badge display
- Alter status badge appearance beyond color token substitution
- Change text styling or font properties

**Preserve:** All component functionality, data visualization, and semantic structure.
</action>
<verify>
<automated> # Verify no hardcoded hex colors remain in campaign/analytics files
grep -oE 'color._:._"#[0-9A-Fa-f]{6}' components/campaigns/CampaignAnalytics.tsx components/campaigns/IdeaStatusBadge.tsx components/factory/FormattedOutput.tsx 2>/dev/null | wc -l # Expected: 0

      # Verify DS tokens are used instead
      grep -l "var(--drp-" components/campaigns/CampaignAnalytics.tsx components/campaigns/IdeaStatusBadge.tsx components/factory/FormattedOutput.tsx

      # Double-check: search for any remaining hex color patterns
      grep '#[0-9A-Fa-f]\{6\}' components/campaigns/CampaignAnalytics.tsx components/campaigns/IdeaStatusBadge.tsx components/factory/FormattedOutput.tsx 2>/dev/null || echo "No hex colors found"
    </automated>

  </verify>
  <done>
- components/campaigns/CampaignAnalytics.tsx contains 0 hardcoded hex colors; all colors use DS tokens
- components/campaigns/IdeaStatusBadge.tsx contains 0 hardcoded hex colors; status colors use DS tokens
- components/factory/FormattedOutput.tsx contains 0 hardcoded hex colors; all colors use DS tokens
- Campaign analytics and status badge display logic preserved
- Color semantics maintained (error = red, success = green, warning = yellow, etc.)
  </done>
</task>

<task type="auto">
  <name>Task 3: Replace colors in onboarding wizard and studio pages</name>
  <files>
    - app/(onboarding)/onboarding/wizard/[step]/page.tsx
    - app/(protected)/studio/page.tsx
  </files>
  <read_first>
    - app/(onboarding)/onboarding/wizard/[step]/page.tsx (identify 5 inline hex colors)
    - app/(protected)/studio/page.tsx (identify 5 inline hex colors)
    - /Users/y/Desktop/doctorpost-v12/CLAUDE.md (DS token reference)
  </read_first>
  <action>
Replace all 10 hardcoded hex colors in these two high-traffic pages with DS tokens.

**app/(onboarding)/onboarding/wizard/[step]/page.tsx:**

- Identify each of 5 inline hex colors
- Map each to appropriate DS token:
  - If color indicates status/progress → use `var(--drp-purple)`, `var(--drp-success)`, etc.
  - If color is text styling → map to appropriate text/muted color
  - If color indicates error/validation → use `var(--drp-error)`
- Replace all hex values with DS tokens
- Preserve onboarding flow display
- Maintain step progress visualization

**app/(protected)/studio/page.tsx:**

- Identify each of 5 inline hex colors
- Determine semantic purpose (status, visualization, annotation, etc.)
- Map to appropriate DS token
- Replace all hex values
- Preserve studio page layout and visualization

**DO NOT:**

- Change onboarding flow logic or step progression
- Modify studio page functionality
- Alter layout or component structure
- Change data visualization beyond color substitution

**Preserve:** Workflow state, UI layout, and information hierarchy.
</action>
<verify>
<automated> # Verify no hardcoded hex colors remain in onboarding and studio pages
grep -oE 'color._:._"#[0-9A-Fa-f]{6}' app/\(onboarding\)/onboarding/wizard/\[step\]/page.tsx app/\(protected\)/studio/page.tsx 2>/dev/null | wc -l # Expected: 0

      # Verify DS tokens used instead
      grep -l "var(--drp-" app/\(onboarding\)/onboarding/wizard/\[step\]/page.tsx app/\(protected\)/studio/page.tsx

      # Double-check for hex patterns
      grep '#[0-9A-Fa-f]\{6\}' app/\(onboarding\)/onboarding/wizard/\[step\]/page.tsx app/\(protected\)/studio/page.tsx 2>/dev/null || echo "No hex colors found"
    </automated>

  </verify>
  <done>
- app/(onboarding)/onboarding/wizard/[step]/page.tsx contains 0 hardcoded hex colors; all colors use DS tokens
- app/(protected)/studio/page.tsx contains 0 hardcoded hex colors; all colors use DS tokens
- Onboarding flow display and studio page functionality preserved
- Color semantics aligned with DS palette
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **Global hex color search** (verify 0 remaining):

   ```bash
   grep -r 'color.*:.*"#[0-9A-Fa-f]\{6\}' \
     app/signup/page.tsx \
     components/brand/sections/VoiceSection.tsx \
     components/factory/DraftEditor.tsx \
     components/campaigns/CampaignAnalytics.tsx \
     components/factory/FormattedOutput.tsx \
     app/\(onboarding\)/onboarding/wizard/\[step\]/page.tsx \
     app/\(protected\)/studio/page.tsx \
     components/campaigns/IdeaStatusBadge.tsx \
     2>/dev/null | wc -l
   # Expected: 0
   ```

2. **DS token adoption:**

   ```bash
   # Count DS color token usage in replaced files
   grep -r "var(--drp-" \
     app/signup/page.tsx \
     components/brand/sections/VoiceSection.tsx \
     components/factory/DraftEditor.tsx \
     components/campaigns/CampaignAnalytics.tsx \
     components/factory/FormattedOutput.tsx \
     app/\(onboarding\)/onboarding/wizard/\[step\]/page.tsx \
     app/\(protected\)/studio/page.tsx \
     components/campaigns/IdeaStatusBadge.tsx \
     2>/dev/null | wc -l
   # Expected: > 40 (at least as many tokens as hex colors replaced)
   ```

3. **No broken styling:**
   - Verify app starts and renders without visual regressions
   - Verify error states display in correct error color
   - Verify status badges show correct colors
     </verification>

<success_criteria>

- [ ] **DS-COLORS-01 ✓**: Error states use `var(--drp-error)` not `#b91c1c` or `#dc2626`
- [ ] **DS-COLORS-02 ✓**: Auth pages (signup, VoiceSection validation) use DS color tokens
- [ ] **DS-COLORS-03 ✓**: All inline hex color violations replaced with `var(--drp-*)` tokens
- [ ] Zero hardcoded hex colors (`#...`) in any of the 8 affected files
- [ ] 40+ DS color tokens used across campaign, analytics, and status badge components
- [ ] Color semantics preserved (error = red, success = green, warning = yellow, muted = grey)
- [ ] All component functionality and data visualization unchanged
      </success_criteria>

<output>
After completion, create `.planning/phases/01-ds-compliance-fixes/01-02-SUMMARY.md` with:
- Hex colors replaced (total count)
- DS tokens adopted (count by token type)
- Files modified (list)
- Ready for Plan 01-03 (borders and typography fixes)
</output>
