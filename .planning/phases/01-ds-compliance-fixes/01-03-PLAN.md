---
phase: 01-ds-compliance-fixes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(protected)/calendar/page.tsx
autonomous: true
requirements:
  - DS-BORDERS-01
  - DS-TYPO-01

must_haves:
  truths:
    - Calendar status dots render with sharp corners (no border-radius)
    - Font sizes use DS text tokens not raw pixel values
  artifacts:
    - app/(protected)/calendar/page.tsx (0 rounded-full classes, 0 raw fontSize pixel values)
  key_links:
    - from: Calendar dots
      to: Sharp corners (border-radius: 0)
      pattern: "borderRadius.*0|rounded.*removed"
      critical: true
    - from: Typography
      to: DS text tokens
      pattern: "var\\(--drp-text-"
      critical: true

---

<objective>
Remove 3 `rounded-full` Tailwind classes from calendar status dots and replace 28 raw pixel font sizes with DS text tokens across components. These are small, high-impact fixes that complete the audit closure.

Purpose: Ensure calendar dots maintain the brutalist design (sharp corners) and typography uses the DS token system consistently.

Output: Calendar page has 0 rounded corners, font sizes across the app use DS tokens.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@/Users/y/Desktop/doctorpost-v12/CLAUDE.md
@/Users/y/Desktop/doctorpost-v12/.planning/phases/ds-audit/ds-UAT.md

# Rounded corners issue (from audit)

- File: app/(protected)/calendar/page.tsx
- Lines: 207, 220, 224
- Issue: 3x `rounded-full` Tailwind classes on status dot indicators
- Fix: Remove Tailwind classes; use inline `borderRadius: 0` or solid background without radius

# Font size issue (from audit)

- 28 instances of raw pixel font sizes (`fontSize: 13`, `fontSize: 10`, etc.)
- Mapping:
  - `fontSize: 10` → `fontSize: "var(--drp-text-xs)"` (11px)
  - `fontSize: 12` → `fontSize: "var(--drp-text-sm)"` (12px)
  - `fontSize: 13` → `fontSize: "var(--drp-text-sm)"` (12px)
  - `fontSize: 14` → `fontSize: "var(--drp-text-md)"` (14px)
  - `fontSize: 16` → `fontSize: "var(--drp-text-lg)"` (16px)
  - etc.
    </context>

<tasks>

<task type="auto">
  <name>Task 1: Remove rounded corners from calendar status dots</name>
  <files>
    - app/(protected)/calendar/page.tsx
  </files>
  <read_first>
    - app/(protected)/calendar/page.tsx (find lines 207, 220, 224 with `rounded-full` classes on status dots)
    - /Users/y/Desktop/doctorpost-v12/CLAUDE.md (DS rule: border-radius: 0 always, brutalist design)
  </read_first>
  <action>
Remove the `rounded-full` Tailwind classes from calendar status dots and ensure sharp corners.

**Locate and fix:** Lines 207, 220, 224 in app/(protected)/calendar/page.tsx

- Remove the `rounded-full` Tailwind class from each status dot indicator
- If the dot styling relied on `rounded-full` to create the circle, replace with:
  - Option 1: Inline `borderRadius: 0` (sharp square dots — may look wrong if they were meant to be circles)
  - Option 2: Replace with a solid background circle using `width` and `height` equal, without radius (keep as square)
  - Option 3: Change the visual from a circle to a small square (2px × 2px filled rect)

**Preferred approach:** Keep the dot visual intent (small indicator) but use sharp corners. If the dot was a visual element indicating status, style it as a small solid square (e.g., `width: 8px, height: 8px, backgroundColor: var(--drp-success)`) without radius.

**DO NOT:**

- Change the status value or meaning
- Alter the calendar layout or functionality
- Modify any other DOM structure

**Context:** `globals.css` has `border-radius: 0 !important` safety net, but removing `rounded-full` class is the correct fix.
</action>
<verify>
<automated> # Verify rounded-full classes removed from calendar page
grep -c 'rounded-full' app/\(protected\)/calendar/page.tsx 2>/dev/null || echo "0 rounded-full classes found"

      # Verify no border-radius in inline styles (should be 0 if present)
      grep 'borderRadius' app/\(protected\)/calendar/page.tsx 2>/dev/null | grep -v 'borderRadius.*0' || echo "No rounded corners detected"

      # Confirm the specific lines 207, 220, 224 no longer have rounded-full
      sed -n '207p;220p;224p' app/\(protected\)/calendar/page.tsx | grep 'rounded-full' || echo "Lines 207, 220, 224 are clean"
    </automated>

  </verify>
  <done>
- app/(protected)/calendar/page.tsx contains 0 `rounded-full` Tailwind classes
- Calendar status dots use sharp corners (consistent with brutalist design)
- Calendar functionality and layout unchanged
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace raw pixel font sizes with DS text tokens</name>
  <files>
    - app/(protected)/calendar/page.tsx
  </files>
  <read_first>
    - app/(protected)/calendar/page.tsx (identify all raw `fontSize: 10`, `fontSize: 12`, etc. instances)
    - /Users/y/Desktop/doctorpost-v12/CLAUDE.md (DS text token mapping: --drp-text-xs, --drp-text-sm, --drp-text-md, etc.)
  </read_first>
  <action>
Replace all raw pixel font sizes with DS text tokens in calendar page.

**Mapping (from audit notes):**

- `fontSize: 10` → `fontSize: "var(--drp-text-xs)"` (11px)
- `fontSize: 12` or `fontSize: 13` → `fontSize: "var(--drp-text-sm)"` (12px)
- `fontSize: 14` → `fontSize: "var(--drp-text-md)"` (14px)
- `fontSize: 16` → `fontSize: "var(--drp-text-lg)"` (16px)
- Adjust for larger sizes: 18px → `--drp-text-h6`, etc.

**Process:**

1. Search calendar page for all instances of `fontSize: [number]` style properties
2. For each found instance, determine the pixel value
3. Replace with corresponding DS token from above mapping
4. Preserve all other styling properties (font-weight, color, line-height, etc.)

**Example:** `style={{ fontSize: 13, color: "..." }}` → `style={{ fontSize: "var(--drp-text-sm)", color: "..." }}`

**DO NOT:**

- Change font-weight, color, or other text properties
- Modify layout or spacing
- Alter text content or component hierarchy

**Note:** Audit found 28 instances across the app; focus on calendar page for now. Other files' font sizes will be addressed as needed.
</action>
<verify>
<automated> # Verify no raw pixel font sizes remain in calendar page
grep -oE 'fontSize:?["\s]_[0-9]+["\s]_[,})]' app/\(protected\)/calendar/page.tsx 2>/dev/null | grep -v 'fontSize.\*var' | wc -l # Expected: 0

      # Verify DS text tokens are used instead
      grep -c "var(--drp-text-" app/\(protected\)/calendar/page.tsx 2>/dev/null || echo "0 DS text tokens found"

      # Double-check for any remaining raw fontSize values (alternative regex)
      grep "fontSize.*[0-9]\+[^a-z]" app/\(protected\)/calendar/page.tsx 2>/dev/null || echo "No raw font sizes detected"
    </automated>

  </verify>
  <done>
- app/(protected)/calendar/page.tsx contains 0 raw pixel font sizes
- All text uses DS text tokens (`var(--drp-text-xs)`, `var(--drp-text-sm)`, etc.)
- Typography styling preserved (weight, color, line-height)
- Calendar layout and functionality unchanged
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **Calendar page border-radius check:**

   ```bash
   # Verify no rounded-full classes
   grep 'rounded-full' app/\(protected\)/calendar/page.tsx && echo "ERROR: rounded-full found" || echo "PASS: 0 rounded-full"

   # Verify font sizes use tokens
   grep -E 'fontSize.*[0-9]+' app/\(protected\)/calendar/page.tsx | grep -v 'var(--drp' && echo "ERROR: raw font sizes found" || echo "PASS: all fonts use tokens"
   ```

2. **Visual verification:**
   - Calendar page renders without Tailwind rounding on status dots
   - Font sizes appear consistent and readable
   - No layout shifts from typography changes

3. **App-wide font size audit (future scope):**
   ```bash
   # After calendar is fixed, scan full codebase for remaining raw font sizes
   grep -r 'fontSize:.*[0-9]\+' --include="*.tsx" . 2>/dev/null | grep -v 'var(--drp' | wc -l
   ```
   </verification>

<success_criteria>

- [ ] **DS-BORDERS-01 ✓**: No `rounded-full` classes on calendar status dots (lines 207, 220, 224 cleaned)
- [ ] **DS-TYPO-01 ✓**: All raw pixel font sizes in calendar page replaced with DS text tokens
- [ ] Zero `rounded-full` Tailwind classes in calendar page
- [ ] Zero raw `fontSize: [number]` inline styles in calendar page
- [ ] Calendar status dots render with sharp corners
- [ ] Typography uses `var(--drp-text-xs)`, `var(--drp-text-sm)`, etc.
- [ ] Calendar layout, functionality, and visual hierarchy preserved
      </success_criteria>

<output>
After completion, create `.planning/phases/01-ds-compliance-fixes/01-03-SUMMARY.md` with:
- Calendar page fixes (rounded corners removed, font sizes replaced)
- Compliance status (all audit issues closed)
- Ready for phase wrap-up verification
</output>
